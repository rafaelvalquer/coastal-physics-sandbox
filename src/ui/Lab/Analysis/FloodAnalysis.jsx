export function FloodAnalysis({result,engine}){const m=result?.metrics||{};return <section className="analysis-detail"><p>Profundidade máxima: <b>{Number(m.maxWaterDepth||0).toFixed(2)} m</b></p><p>Velocidade máxima: <b>{Number(m.maxVelocity||0).toFixed(2)} m/s</b></p><p>Área urbana inundada no pico: <b>{(Number(m.peakFloodedArea||0)*100).toFixed(1)}%</b></p><div className="analysis-overlay-actions"><button onClick={()=>engine?.game?.commandBus?.execute("lab:overlay",{overlay:"MAX_FLOOD_DEPTH"})}>Heatmap profundidade</button><button onClick={()=>engine?.game?.commandBus?.execute("lab:overlay",{overlay:"MAX_VELOCITY"})}>Heatmap velocidade</button><button onClick={()=>engine?.game?.commandBus?.execute("lab:overlay",{overlay:"FLOOD_DURATION"})}>Duração da inundação</button></div></section>;}
