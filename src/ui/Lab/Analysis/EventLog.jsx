function clock(seconds){const s=Math.max(0,Math.round(seconds||0)),h=Math.floor(s/3600),m=Math.floor((s%3600)/60),sec=s%60;return [h,m,sec].map(v=>String(v).padStart(2,"0")).join(":");}
export function EventLog({engine,events=[]}){const focus=e=>{const p=e.payload?.location||e.payload?.position||e.payload;if(Number.isFinite(p?.x)&&Number.isFinite(p?.y))engine?.focusOnWorld?.(p.x,p.y,1.55);};return <div className="lab-event-log">{events.slice().reverse().map(e=><button key={e.id} className={e.severity||"info"} onClick={()=>focus(e)}><time>{clock(e.time)}</time><span>{e.label}</span></button>)}</div>;}
