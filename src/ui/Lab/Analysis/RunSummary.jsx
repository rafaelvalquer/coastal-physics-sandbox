const pct=v=>(Number(v||0)*100).toFixed(1)+"%";const n=(v,d=1)=>Number(v||0).toFixed(d);
export function RunSummary({result}){const m=result?.metrics||{};return <div className="analysis-metric-grid"><div><small>Inundação máxima</small><strong>{pct(m.peakFloodedArea)}</strong></div><div><small>Profundidade máxima</small><strong>{n(m.maxWaterDepth,2)} m</strong></div><div><small>Prédios inundados</small><strong>{Math.round(m.buildingsFlooded||0)}</strong></div><div><small>Prédios destruídos</small><strong>{Math.round(m.buildingsDestroyed||0)}</strong></div><div><small>População exposta</small><strong>{Math.round(m.populationExposed||0)}</strong></div><div><small>Erosão</small><strong>{n(m.erodedVolume)} m²</strong></div></div>;}
