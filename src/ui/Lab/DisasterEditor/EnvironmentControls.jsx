export function EnvironmentControls({environment,onChange}){const saturation=Math.round((environment.soilSaturation||0)*100);return <section className="lab-control-group"><div className="lab-group-title">TERRENO / DURAÇÃO</div><label className="lab-range"><div><span>Saturação inicial</span><b>{saturation}%</b></div><input type="range" min="0" max="1" step=".05" value={environment.soilSaturation||0} onChange={e=>onChange({soilSaturation:Number(e.target.value)})}/></label></section>;}
