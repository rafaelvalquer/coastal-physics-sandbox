export function ChallengeHUD({snapshot}){const c=snapshot?.challenge?.active;if(!c)return null;const evaluation=snapshot?.challenge?.evaluation,editor=snapshot?.editor||{};return <div className="challenge-hud"><div><small>DESAFIO</small><strong>{c.title}</strong></div>{c.budget!=null&&<span>R$ {Math.max(0,c.budget-editor.spent).toLocaleString("pt-BR")} restantes</span>}{evaluation&&<b>{evaluation.completed?"OBJETIVOS CONCLUÍDOS":"REVISE OS OBJETIVOS"}</b>}</div>;}
