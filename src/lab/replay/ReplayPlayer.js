export class ReplayPlayer{constructor({app}){this.app=app;this.time=0;this.playing=false;}load(r){this.recording=r||{events:[],samples:[],checkpoints:[]};this.time=0;this.playing=false;}seek(time){this.time=Math.max(0,Number(time)||0);const c=[...(this.recording?.checkpoints||[])].filter(i=>i.time<=this.time).sort((a,b)=>b.time-a.time)[0];if(c?.state)this.app.hydrateReplayState(c.state);return c;}}