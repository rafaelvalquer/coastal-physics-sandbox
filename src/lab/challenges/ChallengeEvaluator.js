function getPath(source,path){return String(path).split(".").reduce((v,k)=>v?.[k],source);}
function cmp(v,op,e){if(op==="<")return v<e;if(op==="<=")return v<=e;if(op===">")return v>e;if(op===">=")return v>=e;if(op==="==")return v==e;return false;}
export class ChallengeEvaluator{static evaluate(challenge,result,context={}){const source={...result.metrics,metrics:result.metrics,critical:Object.fromEntries((result.criticalInfrastructure||[]).map(i=>[i.id,i])),context};const objectives=challenge.objectives.map(o=>{const value=getPath(source,o.metric);return {...o,value,completed:cmp(value,o.operator,o.value)};});return {completed:objectives.every(o=>o.completed),objectives};}}
