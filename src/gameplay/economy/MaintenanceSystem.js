export class MaintenanceSystem { constructor({budget,eventBus,multiplier=1}){this.budget=budget;this.eventBus=eventBus;this.multiplier=multiplier;} run(items=[]){let paid=0,debt=0;for(const item of items){const base=Number(item.maintenanceCost??(item.cost||0)*(item.maintenance||0))*this.multiplier;if(base<=0)continue;if(this.budget.spend(base,"MAINTENANCE",item.id||item.type)){paid+=base;item.maintenanceDebt=Math.max(0,(item.maintenanceDebt||0)-base*.25);item.condition=Math.min(1,(item.condition??1)+.03);}else{debt+=base;item.maintenanceDebt=(item.maintenanceDebt||0)+base;item.condition=Math.max(0,(item.condition??1)-.04);}}this.eventBus?.emit("maintenance:completed",{paid,debt});return {paid,debt};} }